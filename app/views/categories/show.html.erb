<div class="columns is-multiline">
  <%= @category.name %>
</div>
<h4>Product of this category</h4>

  <%= render partial: 'products/product',
             collection: @category.products,
             locals: { name_is_link: true } %>
